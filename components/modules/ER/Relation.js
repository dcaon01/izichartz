
/**
 * Relation
 * Componente che renderizza una relazione nel modello ER
 * @param id: indice e identificatore dell'elemento all'interno dell'array degli elementi.
*/
export default function Relation({id}) {

    function calculatePoints () {

    }

    return (
        <div>
            <svg>
                <polygon points/>
            </svg>
        </div>
    );
}
