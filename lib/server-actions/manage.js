"use server";

/**
 * Raccolta di server action per la gestione (creazione, eliminazione, modifica) dei file
 * dalla sezione manage del workspace
 */

export async function createProject(prevState, formData) {
    let messages = [];
    return messages.push("porcodio");
}